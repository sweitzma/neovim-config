""" feel
set scrolloff=10                  " keep 3 lines visible above/below the cursor when scrolling
set sidescrolloff=7               " keep 7 characters visible to the left/right of the cursor when scrolling
set ttyfast                       " fast terminal => better redrawing, etc
set mouse=n                       " enable mouse only in Normal mode
